import { styled } from "@mui/material/styles";

export const TitleWrapper = styled("div")(() => ({
  display: "flex",
  alignItems: "center",
  fontSize: "14px",
  lineHeight: "20px",
  gap: "8px",
  marginBottom: "34px",
}));
